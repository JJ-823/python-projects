"""
this was my last attempt at making a python calc
i fixed the last error regarding the Y function
and the number zero. i forgot to put a time
but the dates are below
started at 9/11/2025
finished at 12:32am on 9/12/2025
it didnt take me a whole day this was at the
middle of the night when it turned 12.
"""

def user_input():
    print("pick one of the four functions, by typing in the first letter of the word below. ")
    print("(1 = adding,), (2 = subtracking,), (3 = multiplication,). (4 = division,). ")

    while True:
        choice = input("Type the number here (1, 2, 3, 4): ")
        if choice in ["1", "2", "3", "4"]:
            choice = int(choice)
            break
        else:
            print("choice must be one of the four numbers")

    print("type two numbers below.")
    print("the function will be automatically applied ")
    
    while True:
        try:
            x = int(input("type the (1st) number here: "))
            break
        except ValueError:
            print("pls use a number.")
            continue

    while True:
        try:
            y = int(input("type the (2nd) number here: "))
            break
        except ValueError:
            print("pls use a number.")
            continue

    if choice == 1:
        print(x + y)
        user_input()

    elif choice == 2:
        print(x - y)
        user_input()

    elif choice == 3:
        print(x * y)
        user_input()

    elif choice == 4:
        if y == 0:
            print("dividing a second number as 0 will always be undefined")
        else:
            print(x / y)
        user_input()

user_input()











